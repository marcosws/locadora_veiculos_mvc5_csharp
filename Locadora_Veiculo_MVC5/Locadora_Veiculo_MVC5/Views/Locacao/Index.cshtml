@model IEnumerable<Locadora_Veiculo_MVC5.Locacao>

@{
    ViewBag.Title = "Início";
}

<h2>Início</h2>

<p>
    @Html.ActionLink("Incluir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataLocacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataDevolucao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Automovel.PlacaAutomovel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.NomeCliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataLocacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataDevolucao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Automovel.PlacaAutomovel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.NomeCliente)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id=item.LocacaoID }) |
            @Html.ActionLink("Consulta Detalhada", "Details", new { id=item.LocacaoID }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.LocacaoID })
        </td>
    </tr>
}

</table>
