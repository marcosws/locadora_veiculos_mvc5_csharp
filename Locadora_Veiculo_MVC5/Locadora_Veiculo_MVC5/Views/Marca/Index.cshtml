@model IEnumerable<Locadora_Veiculo_MVC5.Marca>

@{
    ViewBag.Title = "Inicio";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Incluir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeMarca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeMarca)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id=item.MarcaID }) |
            @Html.ActionLink("Consulta Detalhada", "Details", new { id=item.MarcaID }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.MarcaID })
        </td>
    </tr>
}

</table>
